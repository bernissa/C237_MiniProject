<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="/styles.css">
    
    <title>Schedule Planner</title>
</head>
<body>
    <%- include('partials/navbar.ejs') %>
    <div class="container mt-3">
        <h2>Search Results for "<%= query %>"</h2>
        <% if (events.length > 0) { %>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Event</th>
                        <th>Start</th>
                        <th>End</th>
                    </tr>
                </thead>
                <tbody>
                    <% events.forEach(event => { %>
                        <tr>
                            <td><a href="/events/<%= event.eventId %>"><%= event.eventname %></a></td>
                            <td><%= new Date(event.start).toLocaleDateString('en-SG', { weekday: 'short', year: 'numeric', month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit', hour12: false }) %></td>
                            <td><%= new Date(event.end).toLocaleDateString('en-SG', { weekday: 'short', year: 'numeric', month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit', hour12: false }) %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <p>No events found matching the search.</p>
        <% } %>

        <a href="/">Back</a>
    </div>
</body>
</html>