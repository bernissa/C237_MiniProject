<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="/styles.css">
    
    <title>Schedule Planner</title>
</head>
<body>
    <%- include ('partials/navbar.ejs') %>
    <div class="container mt-3">
        <h2>Event Information</h2>

        <% if (event) { %> <!-- Check if event data exists -->
            <div>
                <p><b>Description:</b><br> <%= event.eventname %></p>
                <p><b>Start Time:</b><br> <%= event.start.toLocaleDateString('en-SG', { weekday: 'short', year:"numeric", month:'short', day: 'numeric', hour:'2-digit', minute:'2-digit', hour12: false}) %></p>
                <p><b>End Time:</b><br> <%= event.end.toLocaleDateString('en-SG', { weekday: 'short', year:"numeric", month:'short', day: 'numeric', hour:'2-digit', minute:'2-digit', hour12: false}) %></p>
            </div>
        <% } else { %>
            <p>No event found.</p>
        <% } %>
        <a href="<%= prevpage === 'dailycalendar' ? '/dailycalendar' : '/' %>">Back</a>
    </div>
</body>
</html>